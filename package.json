{
  "scripts": {
    "predeploy" : "npm run build",
    "deploy" : "gh-pages -d build"
  },
  "dependencies": {
    "react-router-dom": "^6.14.1"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
